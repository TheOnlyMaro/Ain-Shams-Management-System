# MongoDB connection for the server (use IPv4 address to avoid IPv6 ::1 issues)
MONGO_URI=mongodb://127.0.0.1:27017/cto_db

# Port the backend should listen on (match VITE_API_URL in frontend .env if needed)
PORT=4000

# Optional
NODE_ENV=development

# Token used by staff clients to authenticate admin actions.
# For local dev you can set a simple string, e.g. "secret-token".
# Requests must include X-Staff-Id and X-Staff-Token headers.
STAFF_TOKEN=secret-token
