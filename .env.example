### Ain-Shams Management System — Example environment file
# Copy this file to `.env` and fill in real secrets. Do NOT commit your real
# `.env` to the repository. Keep production credentials secret (use CI/CD
# secrets or environment-specific configuration in production).

# MongoDB Atlas connection string placeholder.
# Replace <username>, <password>, <cluster> and optionally the database name.
# Example format (Atlas):
# mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/ainshams?retryWrites=true&w=majority

# JWT secret used to sign auth tokens — replace with a long random string
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXP=15m
# Optional: staff token for admissions admin endpoints (used by staff requests)
STAFF_TOKEN=replace_with_staff_token_if_used

# Server port used by the backend
PORT=4000

# Frontend settings (adjust port if your frontend points at a different API URL)
REACT_APP_API_URL=http://localhost:4000/api
REACT_APP_ENV=development

# End of file
